title Part 0 exercise 6 - new note in SPA

note over Browser:
browser executes the event handler
that creates a new note with the user input
and attaches it to the note list
Then sends the user data with a timestamp
to the server in JSON format via a POST request
end note
Browser->Server: HTTP POST https://fullstack-exampleapp.herokuapp.com/new_note_spa
